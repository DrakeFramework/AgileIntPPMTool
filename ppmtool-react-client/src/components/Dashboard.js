import React, { Component } from "react";

/**
 * type rcc and hit tab to automatically generate a template component
 * a class based component is one that can take properties and use lifecycle hooks
 * lifecycle hooks allow you to define how it should be rendered and what shld happen when rendered
 *
 * we can tab into application state when rendered
 */
class Dashboard extends Component {
  render() {
    return <h1>Welcome to the Dashboard</h1>;
  }
}
export default Dashboard;

/**
 * class Dashboard extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}
export default dashboard
 */
